<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="stylesheets/bootstrap.min.css" rel="stylesheet"/>
    <link href="stylesheets/bootstrap-responsive.min.css" rel="stylesheet" />
</head>
<body>
<script src="javascripts/bootstrap.min.js"></script>

<div class="hero-unit">
    <h1>Jumpig 图文</h1>
    <p>专注于图文，让图文更简明</p>
</div>

<div class="page-header">
    <h1><%= rootData.Name%></h1>
</div>

<ul class="nav nav-tabs">
    <% for(var i=0;i< rootData.GroupList.length;i++ ) {%>
        <% if(rootData.GroupList[i].ID== currentGroup.ID) {%>
         <li class="active"><%} else {%><li><%}%><a href="content?id=<%=rootData.GroupList[i].ID%>&topic=<%=ImgDir%>"><%= rootData.GroupList[i].Name || ""%></a></li>
    <% }%>
</ul>

<ul class="thumbnails">
    <% for(var i=0; i< currentGroup.NodeList.length ;i ++) {%>
    <li class="span12">
        <div class="thumbnail">
            <h3>NO.<%= i+1 %></h3>
            <img src="images/<%= ImgDir %>/<%= currentGroup.NodeList[i].Picture %>" alt="200x200" style="">
            <div class="caption">
                <p><%= currentGroup.NodeList[i].Content || "" %></p>
            </div>
        </div>
    </li>
    <% } %>
</ul>

</body>
</html>